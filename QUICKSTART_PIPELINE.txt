╔══════════════════════════════════════════════════════════════════════════╗
║                 PIPELINE ORCHESTRATION - QUICK START                     ║
╚══════════════════════════════════════════════════════════════════════════╝

🚀 ONE-COMMAND END-TO-END EXECUTION

┌──────────────────────────────────────────────────────────────────────────┐
│ Option 1: Quick Test (Recommended First Run)                            │
└──────────────────────────────────────────────────────────────────────────┘

  ./run_pipeline.sh --quick-test

  ✓ Small area (~300m x 300m)
  ✓ 2 transmitters, 50 trajectories
  ✓ 5 training epochs
  ✓ Completes in ~15 minutes
  ✓ Perfect for validation

┌──────────────────────────────────────────────────────────────────────────┐
│ Option 2: Full Pipeline                                                 │
└──────────────────────────────────────────────────────────────────────────┘

  ./run_pipeline.sh --bbox -105.28 40.014 -105.27 40.020 --num-tx 5 --epochs 50

  ✓ Larger area (~1km x 1km)
  ✓ 5 transmitters, 100 trajectories
  ✓ 50 training epochs
  ✓ Production-quality results

┌──────────────────────────────────────────────────────────────────────────┐
│ Option 3: Skip Already-Done Steps                                       │
└──────────────────────────────────────────────────────────────────────────┘

  # Use existing scenes
  ./run_pipeline.sh --skip-scenes

  # Use existing dataset
  ./run_pipeline.sh --skip-scenes --skip-dataset

  # Training only
  ./run_pipeline.sh --train-only

┌──────────────────────────────────────────────────────────────────────────┐
│ What Gets Executed                                                       │
└──────────────────────────────────────────────────────────────────────────┘

  STEP 1: Generate Scenes
    ├─ Download OpenStreetMap data
    ├─ Create 3D building/terrain models
    ├─ Place transmitter sites
    └─ Output: data/scenes/<name>/

  STEP 2: Generate Dataset
    ├─ Run Sionna ray tracing
    ├─ Simulate UE trajectories
    ├─ Extract multi-layer features
    └─ Output: data/processed/<name>_dataset/*.zarr

  STEP 3: Train Model
    ├─ Load synthetic dataset
    ├─ Train transformer model
    ├─ Validate and checkpoint
    └─ Output: checkpoints/<run_name>/*.ckpt

  STEP 4: Generate Report
    └─ Output: checkpoints/<run_name>/pipeline_report.json

┌──────────────────────────────────────────────────────────────────────────┐
│ Common Parameters                                                        │
└──────────────────────────────────────────────────────────────────────────┘

  --bbox WEST SOUTH EAST NORTH    Geographic area
  --num-tx N                      Number of transmitters
  --num-trajectories N            Training trajectories
  --epochs N                      Training epochs
  --batch-size N                  Batch size
  --wandb                         Enable W&B logging
  --clean                         Remove old outputs

┌──────────────────────────────────────────────────────────────────────────┐
│ Help & Documentation                                                     │
└──────────────────────────────────────────────────────────────────────────┘

  ./run_pipeline.sh --help        Full parameter list
  cat PIPELINE.md                 Complete documentation
  cat PIPELINE_SUMMARY.md         Quick reference

┌──────────────────────────────────────────────────────────────────────────┐
│ Example Workflows                                                        │
└──────────────────────────────────────────────────────────────────────────┘

  # First time: validate everything works
  ./run_pipeline.sh --quick-test

  # Generate scenes for your area
  ./run_pipeline.sh --bbox -105.30 40.00 -105.20 40.05 --num-tx 10

  # Experiment with dataset sizes
  ./run_pipeline.sh --skip-scenes --num-trajectories 500

  # Train with different configs
  ./run_pipeline.sh --skip-scenes --skip-dataset --epochs 100 --wandb

  # Production run with tracking
  ./run_pipeline.sh --clean --wandb --run-name "prod_$(date +%Y%m%d)"

┌──────────────────────────────────────────────────────────────────────────┐
│ Output Structure                                                         │
└──────────────────────────────────────────────────────────────────────────┘

  data/scenes/<name>/              ← 3D scenes
  data/processed/<name>_dataset/   ← Training data
  checkpoints/<run_name>/          ← Model weights & config
  pipeline_YYYYMMDD_HHMMSS.log     ← Execution log

╔══════════════════════════════════════════════════════════════════════════╗
║  Ready to start? Run:  ./run_pipeline.sh --quick-test                   ║
╚══════════════════════════════════════════════════════════════════════════╝
