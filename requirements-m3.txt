# M3: Transformer Model Training Requirements
# PyTorch-only, no TensorFlow/Sionna needed at training time

# Core deep learning
torch>=2.0.0
torchvision>=0.15.0
pytorch-lightning>=2.0.0
torchmetrics>=1.0.0

# Vision transformers
timm>=0.9.0

# Experiment tracking
wandb>=0.15.0

# Data loading
zarr>=3.0.0
numcodecs>=0.12.0

# Utilities
pyyaml>=6.0
einops>=0.7.0  # For tensor operations

# M1/M2 dependencies (testing)
-r requirements-test.txt
-r requirements-m2.txt
