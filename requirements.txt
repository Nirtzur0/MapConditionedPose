# Transformer UE Localization - Consolidated Requirements
# Install with: pip install -r requirements.txt

# =============================================================================
# Core Dependencies
# =============================================================================
numpy>=1.24.0
pyproj>=3.6.0  # Coordinate transforms
pyyaml>=6.0    # Configuration files

# =============================================================================
# Deep Learning (PyTorch Stack)
# =============================================================================
torch>=2.0.0
torchvision>=0.15.0
pytorch-lightning>=2.0.0
torchmetrics>=1.0.0
timm>=0.9.0       # Vision transformers
einops>=0.7.0     # Tensor operations

# =============================================================================
# Data Storage & Processing
# =============================================================================
zarr>=2.16.0      # Hierarchical array storage
numcodecs>=0.12.0 # Compression codecs for Zarr
pandas>=2.0.0

# =============================================================================
# Scene Generation Dependencies (Migrated from Geo2SigMap)
# =============================================================================
osmnx>=2.0.0      # OpenStreetMap data
shapely>=2.0.0    # Geometric operations
rasterio>=1.3.0   # Geospatial raster data
open3d>=0.17.0    # 3D geometry processing
triangle          # Delaunay triangulation
pyvista>=0.43.0   # 3D visualization and mesh processing
geopandas>=0.14.0 # Geospatial data processing
tqdm>=4.65.0      # Progress bars
requests>=2.31.0  # HTTP requests
scipy>=1.11.0     # Scientific computing (for interpolation)
importlib_resources>=6.0.0  # Resource file access

# =============================================================================
# Experiment Tracking & Logging
# =============================================================================
wandb>=0.15.0
rich>=13.0.0

# =============================================================================
# Testing
# =============================================================================
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0     # Parallel test execution
pytest-timeout>=2.1.0
pytest-sugar>=0.9.7     # Better test output
pytest-html>=3.2.0      # HTML test reports

# =============================================================================
# Web Interface (Streamlit)
# =============================================================================
streamlit>=1.29.0
plotly>=5.18.0
folium>=0.15.0
streamlit-folium>=0.15.0
pydeck>=0.8.0
Pillow>=10.0.0
python-dotenv>=1.0.0
reportlab>=4.0.0  # PDF export

# =============================================================================
# Optional: Sionna Ray Tracing (Large ~500MB, requires CUDA)
# =============================================================================
# Uncomment if you want to:
# 1. Generate training data from scratch (M2)
# 2. Generate radio maps for physics loss (M4)
#
sionna>=0.15.0
tensorflow>=2.13.0  # Required by Sionna
#
# Note: The system works in mock mode without Sionna for testing
# Precomputed data can be used for training without installing Sionna
